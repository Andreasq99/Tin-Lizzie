// routes/carMetrics.js
const express = require('express');
const router = express.Router();
const { CarMetrics } = require('../models');

// Define routes for CarMetrics model here
// For example, create routes for tracking metrics, updating prices, etc.

module.exports = router;
